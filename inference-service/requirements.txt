fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
pydantic-settings>=2.0
numpy
pandas
statsmodels>=0.14
scikit-learn
sktime>=0.29
chronos-forecasting
# CPU-only torch (this host has no GPU; the default torch wheel drags in ~2.7G of
# nvidia CUDA runtime libs that are never loaded). Install torch separately with
# the CPU index:  pip install torch --index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu
torch
