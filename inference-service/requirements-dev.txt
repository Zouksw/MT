# Dev/test dependencies for the inference service.
# The service itself does not depend on pytest — these are for the test suite only.
# Install into the service venv:
#   source venv/bin/activate && pip install -r requirements-dev.txt
pytest>=8.0
httpx>=0.27        # FastAPI TestClient transport
ruff>=0.6          # lint — CI runs `ruff check .`; absent here broke CI (exit 127)
