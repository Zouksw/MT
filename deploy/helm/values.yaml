# Default values for iotdb-enhanced

replicaCount: 2

image:
  backend:
    repository: iotdb-enhanced/backend
    tag: latest
    pullPolicy: IfNotPresent
  frontend:
    repository: iotdb-enhanced/frontend
    tag: latest
    pullPolicy: IfNotPresent

# Backend configuration
backend:
  port: 3001
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 2000m
      memory: 2Gi
  env:
    NODE_ENV: production
    PORT: "3001"
    AI_NODE_HOST: "iotdb-ainode"
    AI_NODE_PORT: "10810"
  envSecrets:
    - DATABASE_URL
    - REDIS_HOST
    - REDIS_PORT
    - JWT_SECRET
    - SESSION_SECRET

# Frontend configuration
frontend:
  port: 3000
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1Gi
  env:
    NEXT_PUBLIC_API_URL: "http://backend:3001"

# PostgreSQL
postgresql:
  enabled: true
  auth:
    username: iotdb_user
    database: iotdb_enhanced
  primary:
    persistence:
      size: 20Gi
    resources:
      requests:
        cpu: 500m
        memory: 1Gi

# Redis
redis:
  enabled: true
  auth:
    enabled: false
  master:
    persistence:
      size: 5Gi
    resources:
      requests:
        cpu: 250m
        memory: 256Mi

# IoTDB
iotdb:
  enabled: true
  restPort: 18080
  rpcPort: 6667

# Ingress
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: iotdb.example.com
      paths:
        - path: /api
          service: backend
        - path: /
          service: frontend
  tls:
    - secretName: iotdb-tls
      hosts:
        - iotdb.example.com

# HPA
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

# Backup CronJob
backup:
  enabled: true
  schedule: "0 2 * * *"  # Daily at 2 AM UTC
  postgresWal:
    enabled: true
    s3Bucket: ""
  redisRdb:
    enabled: true
    s3Bucket: ""

# Network Policy
networkPolicy:
  enabled: false  # Enable in production for pod isolation
