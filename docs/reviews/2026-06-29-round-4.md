# MT 第 4 轮开发报告 — B4 死代码清理 + C2续 anomalies 抽 service

> **日期**: 2026-06-29 | **轮次**: Round 4

---

## 执行摘要

| 改动 | 类型 | 验证 |
|------|------|------|
| **B4-T1** 零风险死代码删除 | chore | i18n 全套 + social.ts + 6 死组件 + DB DROP 3 表 |
| **B4-T2** 谨慎死函数删除 | chore | alert-rules 6 死函数 + alertNotifications 2 死函数 + 连带测试 |
| **C2续** anomalies 抽 service | refactor | anomalies.ts 682→408 行,7 函数下沉 anomalyService |

---

## B4-T1:零风险死代码清理

### i18n 全套摆设(L4)
`frontend/src/i18n/`(client/navigation/request + 2 messages)、`LanguageSwitcher.tsx`、`next-intl` 依赖全删。next-intl 配置齐全但从未启用(无 middleware、无 [locale] 路由段、唯一消费者 LanguageSwitcher 自身 0 引用)。**删一个"完整但从未启用"的系统**。

### social.ts 孤儿(B1 收尾)
B1 删了 community,social.ts 成孤儿(前端 0 引用 /api/social,3 表全 0 行)。删 routes/social.ts + 3 Prisma model(SharedSignal/SignalComment/SignalLike)+ User 反向关系 + DROP 3 表。

### 6 个死组件
RealTimeChart、DataExport、DataPageStats、GlassCard、StatusBadge、LanguageSwitcher(含 2 个测试文件)。

---

## B4-T2:谨慎死函数删除

### alert-rules.ts 死函数
alert 系统是"半活":list/stats/mark-read/delete alert 这条链路活,但 rule CRUD(create 外)与评估流水线全死。删除:`listAlertRules`/`updateAlertRule`/`deleteAlertRule`/`evaluateAlertRule`/`triggerAlert`/`getActiveAlertRules` + 辅助 `evaluateThreshold`/`sendNotification`。保留 `createAlertRule`/`getAlertRule`(活)。同步删 `services/alerts.ts` re-export + `alert-rules-complex.integration.test.ts`(整文件只测死函数)。

### alertNotifications.ts 死函数
删 `notifyAnomaly`/`notifyForecastReady`(0 引用)。保留 `checkSignalChange`(被 signals.ts 用)和共享 `emitNotification` helper。

### Tier 3(不做)
alert rule 页面(create/rules)缺失端点——这是"本应修复"的功能,需产品决策,不在死代码清理范畴。

---

## C2续:anomalies.ts 抽 anomalyService.ts

### 实施
- **新建** `services/anomalyService.ts`(7 函数):listAnomalies/getAnomaly/detectAnomalies/updateAnomaly/deleteAnomaly/getAnomalyStats/bulkResolveAnomalies。检测算法(z-score/rule-based)封装为 `runDetection` + severity helper
- **anomalies.ts** 682→**408 行**(-274):7 端点委托 service
- **socket.io emit 留路由**:`/detect` 端点的 `io.emit("anomalies:detected")` 是 HTTP/socket 边界,留在路由(service 返回结果,路由 emit)——与 marketService 把 scraper 耦合留路由的模式一致

### 关键决策
- detect 是唯一有多步 Prisma 语义的端点(createMany 异常 → update timeseries → createMany alerts),抽 service 时保持原顺序,**不**顺手加事务(超出范围)
- service 函数接 userId 参数(从 req 提取),不依赖 Express

---

## 质量门

| 检查 | 结果 |
|------|------|
| 后端 tsc / build | ✅ 0 错误 / 成功 |
| 前端 tsc | ✅ 0 错误 |
| 全量测试 | ✅ **238/239 通过(1 skip),零 429** |
| 唯一失败 | "datasets 表无种子数据"(预先存在,非回归) |

测试数 190→**239**(+49):alert-rules-complex 测试已删(-1 文件),但更多 service/middleware 测试纳入运行。净增因 vitest 现在跑更广的 include 范围。

---

## 净变化(估算 -1500~2000 行)
- **删除**:i18n(6文件) + social.ts + 3 social model + 3 DB 表 + 6 死组件 + 2 组件测试 + alert-rules 死函数 + alertNotifications 死函数 + alert-rules-complex 测试
- **新增**:anomalyService.ts(~280行)
- **瘦身**:anomalies.ts -274 行

---

## ROADMAP 指标进展(4 轮累计)
- 胖路由(>600行):6→**3**(community/social 删 + auth/marketData/anomalies 抽 service)<600。剩 metrics(657)/datasets(648)/models(629)
- 路由直连 Prisma:208→**~110**(auth + marketData + anomalies 下沉)
- 死代码:i18n 全套 + social + 8 死组件 + 8 alert 死函数 清除
- 测试 429 假失败:**0** ✅

---

## 下轮预告(第 5 轮)
- **C2续**:metrics(状态单例,需新样板)/datasets/models 抽 service
- **C3**:Next.js patch 升级(修 high 漏洞)
- **持续**:剩余遗留 bug(FE-H1 race guard、FE-H6 credentials、FE-M1 中文硬编码)

---

*本轮 B4 清理 ~1800 行死代码,C2续 推进第三个胖路由重构。三主线持续并行。*
