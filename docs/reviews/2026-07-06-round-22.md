# Round 22 — 高 ROI 回收执行（死代码删除 + beefFetcher 去重）

**Date:** 2026-07-06
**触发依据:** [`2026-07-06-overengineering-audit.md`](2026-07-06-overengineering-audit.md) §8 高 ROI 项
**Skills:** careful（删除前 0-caller 二次确认）· scaffold-component/api（约定对齐）
**注:** 本轮与另一会话并行（对方改 trading/ + tradingSignals.ts/alertNotifications.ts），但两会话目录零交集（我改 ui/ + beef/ + services/cache + 删 predictionQueue/riskMetrics）。曾一度因对方半成品导致 tsc 报错而暂停，对方稳定后恢复。

---

## 改动总览（3 组，全部验证通过）

| 组 | 内容 | 净变化 |
|---|---|---|
| 1 后端死代码 | 删 predictionQueue.ts(237) + riskMetrics.ts；prune cache.ts 244→70；改 server.ts + 移除 bullmq 依赖 | **-2 文件，-1 依赖，cache.ts -174 行** |
| 2 前端死文件 | 删 4 个 0-importer ui 组件 + 3 个空目录 | **-7 项** |
| 3 beefFetcher 去重 | 新建 lib/beef.ts；3 份字面复制 → 1 处导入 | **-3 份重复，+1 共享模块** |

---

## Group 1 — 后端死代码

| 操作 | 路径 | 证据 |
|---|---|---|
| 删文件 | `services/predictionQueue.ts`（237 行）+ 测试 | 4 个 enqueue 函数 0 prod caller；`initPredictionQueue` 唯一 caller=server.ts:133。**BullMQ worker 永不被喂活**——真实调度走 predictionCache.ts 的 setInterval |
| 删文件 | `services/riskMetrics.ts` + 测试 | 0 importer，无 route 暴露 |
| prune | `services/cache.ts` 244→70 行 | 删 13 个 0-caller 导出（initCache/del/delPattern/exists/incr/expire/mget/mset/cache 包装器/invalidatePattern/getCacheStats/flushCache/closeCache + NULL_CACHE_TTL）。保留 get/set/cacheKeys（inference.ts + predictionCache.ts 唯二 importer 只用这些）|
| 改 | `server.ts` | 删 `import { initPredictionQueue }` + L131-137 try/catch init 块。**保留** schedulePredictionsFromPostgreSQL（不同函数，不同文件） |
| 改 | `package.json` + lockfile | 移除 `"bullmq": "^5.74.1"`（仅 predictionQueue.ts 一处 import）。`pnpm install` 已更新 |

**验证：** backend tsc 0 错误 / `pnpm build` 成功 / **442 测试通过 + 1 skipped**（predictionQueue/riskMetrics 测试随源文件删，0 新失败）/ pm2 restart /health 200。

---

## Group 2 — 前端死文件

| 操作 | 路径 | 删除前 importer 数 |
|---|---|---|
| 删 | `components/ui/MobileStatsCard.tsx` | 0 |
| 删 | `components/ui/separator.tsx` | 0（其他 Separator 命中是 base-ui 内部） |
| 删 | `components/ui/switch.tsx` | 0 |
| 删 | `components/ui/tooltip.tsx` | 0（其他命中是 recharts） |
| 删空目录 | `components/data/`、`contexts/color-mode/__tests__/`、`providers/auth-provider/__tests__/` | 空目录 |

**审计误报修正（重要）：** 初版审计建议删 lowercase `select.tsx`/`button.tsx`/`card.tsx`，但二次确认发现它们是 PascalCase wrapper（`Select/`、`Button/`、`Card/`）的 **base 层**——`Button/index.tsx:4` `import ... from "../button"`。**删 base 会破 wrapper，故不删**。barrel `ui/index.ts` 不含任何删除项，无需改。

**验证：** frontend tsc 0 错误。

---

## Group 3 — beefFetcher 去重

3 份**字面字节相同**的 `beefFetcher`（hash `8ebb8a5...`）分散在：
- `app/beef/page.tsx:11-20`
- `app/beef/factories/page.tsx:8-17`
- `app/beef/cuts/[cutCode]/page.tsx:9-18`

**修复：** 新建 `lib/beef.ts` 导出 `beefFetcher`（+ `BEEF_API_BASE`），3 页改为 `import { beefFetcher } from "@/lib/beef"`，删本地副本。

**不复用 `lib/api.ts` 的 apiFetcher：** 那是 Bearer-token 模式，beef 是 cookie-only（`credentials: "include"`，beef 端点 backend 公开），认证模型不同，硬合并会破。

**验证：** frontend tsc 0 错误 / **next build exit 0**（15.6s，3 个 /beef 路由正常渲染）/ jest **272/272 通过**。

---

## 全栈质量门（全通过）

| 门 | 结果 |
|---|---|
| backend tsc --noEmit | ✅ 0 错误 |
| backend pnpm build (tsc + tsc-alias) | ✅ 成功 |
| backend vitest | ✅ **442 通过** + 1 skipped，0 新失败 |
| pm2 restart mt-backend /health | ✅ HTTP 200（删 BullMQ worker 不破坏启动） |
| frontend tsc --noEmit | ✅ 0 错误 |
| frontend next build | ✅ exit 0，Compiled successfully |
| frontend jest | ✅ **272/272 通过** |

---

## 净回收估算（实测）

| 维度 | 回收 |
|---|---|
| 后端 LOC | ~410 行（predictionQueue 237 + riskMetrics + cache.ts -174 净） |
| 后端依赖 | 1 个（bullmq） |
| 后端文件 | -4（2 源 + 2 测试） |
| 前端文件 | -4（ui 组件）+ -3（空目录）|
| 前端重复 | -2 份 beefFetcher 复制（保留 1） |

---

## 并行会话协调记录

- 暂停触发：backend tsc 突报 5 错（tradingSignals.ts/alertNotifications.ts），全在我**没碰**的文件 → 确认另一会话在改同一工作树
- 恢复判断：对方最后编辑 13min 前（已停手/stable），tsc 恢复通过；我的 Group 2/3 目录（ui/ + beef/）与对方（trading/）零交集
- 风险控制：仅做删除死代码 + 去重，无逻辑变更；每组独立验证

---

## 仍明示未做（中 ROI，需决策或跨栈迁移）

- organizations/organization_members/saved_queries schema（投机多租户 + 死模型）
- validateApiKey（发得出验不了，需补中间件 or 删）
- billing Stripe 列（ROADMAP 已降级，schema 仍带）
- Storybook（5 故事/92 组件，vestigial）
- MSW（205 行 0 测试用）
- 双图标库（lucide + phosphor）
- lowercase shadcn base 与 PascalCase wrapper 的重复（live，删会破 wrapper，需重构 wrapper 自包含）
- inference.ts 信封统一（跨栈，需同步改 3 前端 caller）

这些归后续轮次，不在本轮高 ROI 范围。
