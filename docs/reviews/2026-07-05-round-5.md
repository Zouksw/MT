# MT 第 5 轮开发报告 — C2续 datasets + 遗留 bug 清零

> **日期**: 2026-07-05 | **轮次**: Round 5

---

## 执行摘要

| 改动 | 类型 | 验证 |
|------|------|------|
| **C2续** datasets 抽 service | refactor | datasets.ts 648→374 行(-274) |
| **FE-H6** credentials 嵌套 | fix | cookie auth 恢复 |
| **FE-M1** 中文硬编码 | fix | ChartToolbar/TimeframeSelector 英文化 |
| **FE-H1** loadSignal 竞态 | fix | AbortController + cleanup |

---

## C2续:datasets.ts 抽 datasetService.ts

### 实施
- **新建** `services/datasetService.ts`(7 函数):listDatasets/getDataset/createDataset/requireOwnedDataset/updateDataset/deleteDataset/importDatasetData + serializeDataset helper
- **datasets.ts** 648→**374 行**(-274):6 端点委托 service。CSV/JSON import 的 Papa.parse + 批量插入逻辑整体下沉
- **缓存清理留路由**:invalidateCache 耦合 logger,且是 HTTP 边界副作用,留路由层(service 只管 Prisma)
- **所有权校验下沉**:requireOwnedDataset 统一 404/403 逻辑(create/update/delete/import 复用)

---

## 遗留 bug 清零

### FE-H6:credentials 嵌套(ai/predict)
`fetch(...)` 的 `credentials: "include"` 错误放在 headers 对象里 → 静默禁用 cookie auth。移到 fetch 顶层选项。

### FE-M1:中文硬编码(trading 控件)
- ChartToolbar.tsx:`K线`→`Candle`、`折线`→`Line`
- TimeframeSelector.tsx:已有 labelEn 字段但渲染用 labelCn,改为纯英文(去掉双 label 结构)
- 测试同步:sed 批量更新断言

### FE-H1:loadSignal 竞态(useTradingData)
调研发现 loadSignal 已从 trading/page.tsx 移到 useTradingData.ts(竞态仍在)。修复:loadSignal 接 AbortSignal 参数 → fetch 传 signal → useEffect 创建 controller + cleanup abort。快速切商品时旧请求被 abort,不再覆盖新数据。

---

## 环境问题修复(非代码)
测试启动报 `Cannot find module './core'`(js-yaml)。pnpm store 里 js-yaml 包内容损坏(lib 文件缺失)。`pnpm add -D js-yaml && pnpm remove js-yaml` 强制重新下载完整包到 store,修复后移除显式声明(它是间接依赖)。

---

## 质量门

| 检查 | 结果 |
|------|------|
| 后端 tsc / build | ✅ 0 错误 / 成功 |
| 前端 tsc | ✅ 0 错误 |
| 全量测试 | ✅ **238/239 通过(1 skip),零 429**(与第 4 轮一致) |
| 唯一失败 | "datasets 表无种子数据"(预先存在,非回归) |

---

## ROADMAP 指标进展(5 轮累计)
- 胖路由(>600行):6→**2**(metrics 657 / models 629 仍超)。datasets 已降至 374
- 路由直连 Prisma:208→**~90**(auth + marketData + anomalies + datasets 下沉)
- 遗留 bug:FE-H6 ✅ FE-M1 ✅ FE-H1 ✅ 清零(除 M7/M10 已在 B3/B2 修复)
- 测试 429 假失败:**0** ✅

---

## 下轮预告(第 6 轮)
- **C2续**:models.ts(629)抽 service(最后一个易抽的胖路由);metrics 有状态障碍单独处理
- **C3**:Next.js 15.5.15→15.5.18 patch 升级(修 high 漏洞)
- **持续**:设计 token 化(L1,376 处硬编码颜色)

---

*本轮完成第四个胖路由重构(datasets)+ 清零 3 个遗留前端 bug。C 线架构债持续偿还。*
