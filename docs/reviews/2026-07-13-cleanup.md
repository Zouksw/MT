# Project Cleanup — 统一状态、删除冗余

**Date:** 2026-07-13
**Status:** ✅ Complete — backend 431/431, frontend 307/307, both builds exit 0

## Summary

Three-way deep audit (frontend / backend / config+docs) identified ~60 redundant
items. Removed all verified-dead code, fixed 2 broken configs, archived 29 stale
review docs, and unified the product identity. **Zero functional loss** — every
deletion was grep-verified to have zero external references.

## C1 — Frontend dead code (9 files + 2 partial trims)

**Deleted (zero importers):**
- `components/header/` (dead 148-line Header, superseded by AppShell)
- `components/ThemeToggle.tsx`, `components/ui/dropdown-menu.tsx` (only importer was header/)
- `lib/logger.ts`, `lib/auth.ts` (orphan modules, zero imports)
- `lib/accessibility/` (220-line WCAG color utils, never called)
- 4 dead barrel `index.ts`: `ui/ErrorDisplay/`, `ui/LoadingState/`, `ui/Toast/`, `ui/OnlineStatus/`

**Trimmed to live exports only:**
- `ui/Skeleton/index.tsx`: kept `StatsCardSkeleton`, removed 5 unused skeletons
- `lib/animations.ts`: kept `shouldReduceMotion`, removed ~15 unused exports

## C2 — Frontend orphan pages (conservative, 6 dirs)

Deleted create/edit pages with zero nav entry (lists use in-page modals instead):
`alerts/create`, `alerts/edit`, `anomalies/create`, `anomalies/edit`, `apikeys/create`.
**Kept** show pages + analysis-domain pages (models/performance/backtest) for future nav wiring.

## C3 — Backend dead code (12 files + 6 dead exports)

**Deleted:** `schemas/alerts.ts`, `schemas/timeseries.ts` (routes use service schemas),
`schemas/__tests__/schemas.test.ts` (tested deleted schemas), `test-helpers.ts` (@deprecated),
7 dead `test/helpers/*` (auth/cleanup/createTestApp/integrationApp/testDatabase/datasets-test-helper/index),
`test/factories/`, `test/fixtures/` (zero callers), `src/scripts/seed.ts` (no-op stub).
**Kept** `test/helpers/testApp.ts` (12 importers) + `testContext.ts` (4 importers).

**Dead exports removed:** `response.ts` (responseUtils/notFound/unauthorized/conflict),
`schemas/common.ts` (PaginationParams), `schemas/datasets.ts` (datasetsQuerySchema),
`schemas/models.ts` (forecastsQuerySchema).

## C4 — Config cleanup (fixed 2 broken + removed dead entries)

- **next.config.mjs**: added `output: 'standalone'` (fixes Dockerfile.frontend broken COPY),
  removed dead `transpilePackages:["@refinedev/antd"]` + 5 dead optimizePackageImports entries
  (antd/@ant-design/@phosphor-icons/refinedev — none installed) + dead antd splitChunks group
- **tailwind.config.ts**: removed dead `src/pages/**` content path (App Router, no pages dir)
- **Dockerfile.frontend**: node:18→node:20 (matches CI + backend)
- **`.prettierrc`/`.prettierignore`**: deleted (biome is the active formatter; prettier isn't a dep)
- **ecosystem.config.cjs**: removed redundant `dotenv_config_path` (cwd already backend/)
- **.gitignore**: deduplicated `backups/` + `.wget-hsts`

## C5 — Docs + deps

- **Archived** 29 pre-2026-07-12 reviews → `docs/archive/` (rounds 1-24, sarimax, overengineering audit)
- **Deleted** `docs/blog/` (stale IoTDB/"TradeMind AI enterprise TSDB" identity), `deploy-blog.yml` (broken step ref), `archive/ai-node-setup.md`
- **INDEX.md**: rewrote Reviews section to point at kept docs only; updated test counts (431/307)
- **README.md**: unified product identity to beef-trade platform (was "大宗商品 108 商品"); fixed AI model count (5, not 6/7/8); removed Socket.IO claim (unused); updated page list + API examples to beef-focused
- **frontend/package.json**: removed `socket.io-client` (zero imports)

## Verification
- Backend: tsc clean, vitest **431/431** (36 files)
- Frontend: tsc clean, next build **exit 0** (standalone output confirmed), jest **307/307** (23 files)
- Live: backend serves /api/beef/cuts + /api/news (5 articles), frontend HTTP 200

## Deferred (explicitly out of scope)
- 92 ad-hoc `toFixed` → lib/format migration (large, separate round)
- 3 inline StatCard → shared ui/StatCard (per-page variant verification)
- lib/logger.ts ↔ utils/logger.ts dual-path unification (wide blast radius)
- Internal-use middleware exports (`export` keyword removal — pure style)
